version: "3.8"
services:

    # Nginx service
    nginx:
        image: nginx:latest
        ports:
            -   8080:8080
        volumes:
            - ./nginx.conf:/etc/nginx/conf.d/default.conf

